#!/usr/bin/env node
import fs from 'node:fs';
import path from 'node:path';

const repo=process.cwd();
const dawn=path.join(repo,'static/dawn-library');
const canonicalDir=path.join(dawn,'canonical');
const acquisitionDir=path.join(dawn,'acquisition','chinese');
const outDir=path.join(dawn,'editorial','chinese-acquisition');
fs.mkdirSync(outDir,{recursive:true});
const norm=s=>String(s||'').normalize('NFKC').toLowerCase().replace(/[\s\p{P}\p{S}]+/gu,'').trim();
const canonicalRoot=JSON.parse(fs.readFileSync(path.join(canonicalDir,'root.json'),'utf8'));
const byTitle=new Map(),byTitleAuthor=new Map();let canonicalWorks=0;
for(const shard of canonicalRoot.shards||[]){const p=JSON.parse(fs.readFileSync(path.join(canonicalDir,shard.href),'utf8'));const raw=p.works??p.items??p.workRefs??p;for(const w of (Array.isArray(raw)?raw:Object.values(raw||{}))){canonicalWorks++;const id=w.workId||w.id||w.canonicalId,title=w.title||w.name||w.label||'',authors=[...(Array.isArray(w.authors)?w.authors:[]),...(w.author?[w.author]:[])].map(a=>typeof a==='string'?a:(a?.name||a?.label||'')).filter(Boolean),nt=norm(title);if(nt){if(!byTitle.has(nt))byTitle.set(nt,[]);byTitle.get(nt).push(id)}for(const a of authors){const k=`${nt}|${norm(a)}`;if(nt&&norm(a)){if(!byTitleAuthor.has(k))byTitleAuthor.set(k,[]);byTitleAuthor.get(k).push(id)}}}}
const files=fs.existsSync(acquisitionDir)?fs.readdirSync(acquisitionDir).filter(n=>/\.(json|jsonl)$/i.test(n)).sort():[],rows=[];
for(const file of files){const text=fs.readFileSync(path.join(acquisitionDir,file),'utf8');let data=[];if(file.endsWith('.jsonl'))data=text.split(/\r?\n/).filter(Boolean).map(JSON.parse);else{const j=JSON.parse(text);data=Array.isArray(j)?j:(j.candidates||j.items||j.rows||[])}for(const c of data){const title=c.title||c.name||c.label||'';if(!title)continue;const author=typeof c.author==='string'?c.author:(c.author?.name||c.primaryAuthor||''),nt=norm(title),key=`${nt}|${norm(author)}`,exact=author?(byTitleAuthor.get(key)||[]):[],titleMatches=byTitle.get(nt)||[];let identityDisposition='NEW_WORK_CANDIDATE',matches=[];if(exact.length){identityDisposition='MATCH_EXISTING';matches=exact}else if(titleMatches.length){identityDisposition='EDITION_OR_RESOURCE';matches=titleMatches}let admissionDisposition=identityDisposition;if(c.policyRejected===true)admissionDisposition='POLICY_REJECTED';else if((c.rightsStatus==='deferred'||c.rights==='deferred')&&identityDisposition==='NEW_WORK_CANDIDATE')admissionDisposition='RIGHTS_DEFERRED';rows.push({sourcePool:c.sourcePool||c.pool||c.source||file,title,author,year:c.year||c.date||null,language:c.language||c.languages||'zh',resourceType:c.resourceType||c.type||'text',providerId:c.providerId||c.id||null,providerUrl:c.providerUrl||c.url||null,rightsStatus:c.rightsStatus||c.rights||'unknown',normalizedTitle:nt,identityDisposition,admissionDisposition,canonicalMatches:matches})}}
const identityCounts={},admissionCounts={};for(const r of rows){identityCounts[r.identityDisposition]=(identityCounts[r.identityDisposition]||0)+1;admissionCounts[r.admissionDisposition]=(admissionCounts[r.admissionDisposition]||0)+1}
const report={schema:'dawn.chinese-acquisition.materialization.v2',generatedAt:new Date().toISOString(),canonicalWorksScanned:canonicalWorks,inputFiles:files,candidateRows:rows.length,identityCounts,admissionCounts,rows};fs.writeFileSync(path.join(outDir,'candidate-materialization-v1.json'),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify({canonicalWorksScanned:canonicalWorks,inputFiles:files.length,candidateRows:rows.length,identityCounts,admissionCounts},null,2));
