# DORÉ BENCHMARKS SOURCE-FAMILY RECONCILIATION — 2026-09-14

Status: BOUNDED_RECONCILIATION_COMPLETE
Parent: `DORÉ-MEMORY-CONSOLIDATION-SWEEP-01.md`
Canonical register: `DORÉ-MASTER-WORK-REGISTER.md`
P01 impact: NONE

## Source family reviewed

The complete current `dore-core/benchmarks/` family contains one artifact:

- `RESEARCHER-GRADUATION.md` — status `BENCHMARK SPECIFICATION`.

This pass also reconciled that specification against the already-durable Researcher bounded completions in `DORÉ-COMPLETED-WORK-LEDGER.md` and the canonical CORE evidence boundary in `DORÉ-MASTER-WORK-REGISTER.md`.

## Classification

`RESEARCHER-GRADUATION.md` is `CORE/CONTINUOUS / BENCHMARK SPECIFICATION`.

It is not a completed Researcher graduation, not a course completion artifact, and not evidence that a global Researcher capability threshold has been met.

## Findings

1. The benchmark's governing epistemic envelope remains aligned with current Doré doctrine: answer, canonical context, evidence, sources, alternative views, confidence, uncertainty, Westside relevance, provenance and human-review need must remain distinguishable.
2. Its ten benchmark families are broader than any single completed Researcher course. Existing bounded completions such as Researcher 01, 02 and 04 prove their own contracts only; they do not silently satisfy the whole Researcher Graduation benchmark.
3. The file deliberately refuses to invent an arbitrary numeric graduation threshold before a real benchmark dataset exists. That remains the correct evidence posture.
4. Therefore a global `RESEARCHER_GRADUATE`-style claim remains `UNKNOWN_NEEDS_EVIDENCE` unless a real held-out benchmark dataset, scoring/review method, human-reviewed threshold and persisted results are added later.
5. No contradiction or supersession was found. The benchmark should be retained as the canonical high-level Researcher evaluation specification while course-specific exams remain separate historical evidence.
6. No completed-work revisit, retirement or superseded item is created by this family.
7. No P01 file, runtime state, credential, deployment, binding, blocker or resume condition was changed.

## Smallest future proof for global graduation

Create a representative held-out benchmark dataset spanning all ten families, define scoring only from that real dataset, require both factual/research accuracy and epistemic-discipline failures to be visible, include human review for consequential graduation judgment, and persist the complete evaluation artifact. Until then, do not mint a global Researcher graduation token from narrative completion or course aggregation.

## Durable disposition

- Benchmark specification: retain as `CORE/CONTINUOUS`.
- Existing course completions: retain their bounded `VERIFIED_COMPLETE` classifications.
- Global Researcher graduation: `UNKNOWN_NEEDS_EVIDENCE`.
- Sweep 01: remains `ACTIVE_PARALLEL`.
- P01: unchanged `BLOCKED / ENVIRONMENT_BLOCKED` under the existing production audio/transcription dependency.
