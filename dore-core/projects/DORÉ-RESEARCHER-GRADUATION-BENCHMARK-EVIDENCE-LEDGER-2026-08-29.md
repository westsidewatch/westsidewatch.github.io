# DORÉ RESEARCHER GRADUATION BENCHMARK EVIDENCE LEDGER — 2026-08-29

Status: SWEEP-01 RECONCILED
Source family: `dore-core/benchmarks/`
Primary index: `dore-core/projects/DORÉ-MASTER-WORK-REGISTER.md`

## Bounded evidence reviewed

- complete current `dore-core/benchmarks/` family;
- `dore-core/benchmarks/RESEARCHER-GRADUATION.md`;
- current Master Register `CORE` and Researcher capability-state interpretation;
- previously reconciled bounded Researcher 01/02/04/05/06 completion evidence.

## Finding

The benchmark file is a **benchmark specification**, not a completed graduation artifact. It defines ten research families and an epistemic response envelope, but explicitly states that a numeric threshold will be defined only after the first real benchmark dataset exists and that graduation requires both accuracy and epistemic discipline reviewed by human authority.

No artifact in this bounded family supplies:

- the first real benchmark dataset;
- a defined scoring/threshold policy derived from that dataset;
- one aggregate execution across the ten benchmark families;
- a human-reviewed final graduation judgment under this benchmark contract.

Therefore the benchmark cannot be used to claim a global `RESEARCHER_GRADUATE` state.

## Relationship to completed Researcher courses

The already reconciled Researcher 01/02/04/05/06 milestones remain legitimate bounded competency/course completions. They demonstrate specific capabilities such as ONE evidence-boundary discipline, research-method transfer, autonomous learning, concept development and noise-aware retrieval.

Those completions do **not** silently satisfy the broader benchmark because the benchmark contract is distinct and contains its own dataset/threshold/human-review requirements.

Conversely, the absence of a global benchmark graduation does not invalidate the bounded course completions.

## Current classification

- `RESEARCHER-GRADUATION.md`: `ACTIVE / BENCHMARK SPECIFICATION`.
- Global Researcher graduation under this benchmark: `UNKNOWN_NEEDS_EVIDENCE`.
- Bounded Researcher 01/02/04/05/06 milestones: retain their existing verified historical classifications.
- Researcher capability overall: remain under `CORE/CONTINUOUS` retention and real-work transfer; do not manufacture a global graduation token merely to close the benchmark.

## Smallest useful future proof

Only when a global Researcher graduation becomes operationally useful:

1. build the first representative benchmark dataset across the ten families;
2. define scoring and threshold rules from observed difficulty rather than inventing an arbitrary percentage;
3. run the benchmark with provenance/confidence/uncertainty/human-review fields preserved;
4. persist failures and abstentions as evidence, not only successes;
5. obtain the benchmark's required human-authority review;
6. issue a global graduation state only if that exact contract passes.

## Current priority judgment

LOW/MEDIUM. The benchmark is valuable as an epistemic north-star, but there is no evidence that issuing a global Researcher graduation is presently necessary for P01 or another active critical path. Real-work retention/transfer should remain higher-value evidence.

## P01 isolation

No P01 code, runtime state, deployment, binding, credential, ordering, priority or blocker state was modified.
