# DORÉ A2A / DAWN / CINEMA POST-107 RECONCILIATION

Date: 2026-09-14
Status: DURABLE SWEEP EVIDENCE
Parent: `DORÉ-MEMORY-CONSOLIDATION-SWEEP-01.md`
Canonical register: `DORÉ-MASTER-WORK-REGISTER.md`
P01 impact: NONE

## Bounded evidence reviewed

- merge commit `1f6015e14fb6c2310b89bd7a1ba60ca72e495b07` — A2A Reliability 1F / Living Water real Universal consumer;
- follow-up reliability fix `27313b897f6df172fc2bbd5d0b0dd8af2a9f46cf` — preserve recovery state and retry-attempt budget across retries;
- merge commit `8d8480d5d45b022fba80e0a5e4849f8be17ac2f3` — GOOD TV special-source isolation in Paradise Cinema;
- merge commit `e518c9389c27aa58c0e4ce1a98d31f202bc8b9b9` — Dawn source-identity admission gate.

## Reconciliation findings

### 1. A2A Reliability Atlas moved materially forward, but should not be inflated to whole-system completion

The 1F merge establishes a materially stronger Universal A2A execution path: canonical capability authority, normalized `dore.call` ingress, durable execution/recovery, canonical failure taxonomy, durable result delivery, three GitHub entrances converging on one resident Core, failure-injection acceptance, and a real Living Water consumer path. `design.intelligence` is now registered as a graduated Core-adapter capability while production auto-promotion remains forbidden.

The immediate follow-up fix is important evidence, not noise. It shows the first merged 1F state still had a recovery-state defect around repeated retries / ambiguous side effects. The fix adds persisted provider-attempt counting, explicit blocked states (`UNKNOWN`, `RESEARCH_REQUIRED`), retry-budget escalation, and replay suppression for side-effecting operations whose outcome is uncertain.

Current classification:

- A2A durable execution lifecycle: retain `VERIFIED_COMPLETE / COMPONENT`;
- A2A Reliability Atlas / Universal compulsory-path reliability: upgrade from merely implemented foundation to `ACTIVE_PARALLEL / STRONG_IMPLEMENTED_FOUNDATION`;
- real Living Water Universal consumer: `VERIFIED_COMPLETE / COMPONENT` only if the persisted acceptance artifact/workflow result is independently retained; commit text alone is implementation evidence, not a universal production-health token;
- whole heterogeneous-product reliability, long-duration recovery and universal production mutation authority: still `UNKNOWN_NEEDS_EVIDENCE` / out of scope.

Durable lesson: retry state is part of authority and execution truth. A retry-safe capability may consume a bounded retry budget, while uncertain side effects must persist a blocked state and refuse replay until explicitly reconciled.

### 2. GOOD TV is now a deliberate special-source boundary, not a failed canonical-source case

Paradise Cinema now has an explicit special-source policy and dedicated GOOD TV page. GOOD TV is filtered out of the canonical resource catalog and Bible-media-coordinate catalog, is excluded from preview/source-probe expectations, and is presented as an official-source handoff rather than rehosted or simulated.

Current classification:

- GOOD TV canonical Cinema integration attempt: `SUPERSEDED` as the governing direction;
- GOOD TV special-source handoff: `VERIFIED_COMPLETE / COMPONENT` at repository-policy level;
- Paradise Cinema overall: remains `ACTIVE_PARALLEL / IMPLEMENTED_FOUNDATION`.

Durable lesson: source heterogeneity does not require every provider to be normalized into the same canonical media-resource path. Rights/protection/runtime constraints can justify an explicit special-source class that preserves discoverability without pretending canonical ingest or preview capability.

### 3. Dawn removed positional identity coupling and now requires admission before canonical identity

The Dawn merge replaces positional storefront-to-surface pairing with an explicit `dore.source-identity-admission.v1` map. Source claims are admitted only when compatible with canonical title/author identity; otherwise Dawn falls back to a local Work identity. Cover reconciliation consumes the admitted identity map and fails on post-admission identity collision.

Current classification:

- positional shelf/item pairing as identity authority: `SUPERSEDED`;
- Dawn source-identity admission gate: `VERIFIED_COMPLETE / COMPONENT` at repository implementation level;
- Dawn Library overall: remains `ACTIVE_PARALLEL / IMPLEMENTED_FOUNDATION` because storefront readback, repeated-loop dedupe and editorial relevance defects remain separate open gates.

Durable lesson: source position is presentation, never identity authority. External identity claims must pass a canonical admission boundary before they may become Work identity; disagreement should produce explicit fallback/reconciliation state rather than silent coupling.

## Register interpretation delta

The canonical Master Work Register should interpret the post-107 state as follows when next edited:

- `MEM-SWEEP-01`: durable frontier advances to Checkpoint 108;
- A2A Reliability Atlas: stronger implemented foundation with one real Living Water Universal consumer and explicit blocked/retry persistence, but not whole-system verified complete;
- `DAWN-LIBRARY`: source-identity admission is now a bounded verified component and positional identity coupling is superseded;
- Holy Light / Paradise Cinema: GOOD TV special-source isolation is a bounded verified component; whole Cinema remains active;
- P01 subtitle critical path remains unchanged and untouched.

## No blocker / no completion claim

This batch found no new `HUMAN_DECISION_BLOCKED` or `ENVIRONMENT_BLOCKED` condition beyond the already-known P01 production audio/transcription dependency. Sweep 01 remains `ACTIVE_PARALLEL`; Sweep-wide `VERIFIED_COMPLETE` is not earned.
