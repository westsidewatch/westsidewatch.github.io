# DORÉ MASTER WORK REGISTER — ADDENDUM: IMAGE → DESIGN NATIVE PLACEMENT

Date: 2026-09-05
Status: CANONICAL_ADDENDUM / SWEEP_RECONCILED
Parent register: `DORÉ-MASTER-WORK-REGISTER.md`
Evidence: `DORÉ-IMAGE-DESIGN-NATIVE-PLACEMENT-EVIDENCE-LEDGER-2026-09-05.md`
P01 impact: NONE

## Canonical interpretation

This addendum extends the current `CAPABILITY-RUNTIME`, `VIS-GRAMMAR`, Doré Image, Doré Design and A2A/control-plane interpretation until safely folded into the giant Master Work Register.

### IMAGE-DESIGN-NATIVE-PLACEMENT

**Status:** `ACTIVE_PARALLEL / VERIFIED_COMPLETE_SUBMILESTONE`

A concrete native visual-state path now exists in repository implementation:

`generated image artifact → DesignAsset → DesignImageShape → Doré Design workspace → deterministic render/verification`

Doré Design is promoted from synthetic-only visual-consumer behavior to a resident production-capability consumer for real mutations. Real mutation paths fail closed when the Mac Design service is unavailable, require workspace revision advance, and verify the resulting workspace/render state.

The governing visual-state model is renderer-neutral: Doré-owned workspace/application state remains authoritative; asset identity and image-shape geometry are separate; future canvas engines are replaceable projections rather than persistent state owners.

### Superseded / retired directions

- manual generated-image download/upload between Doré Image and Doré Design: `SUPERSEDED`;
- separate Image and Design agents exchanging prose as the normal production handoff: `SUPERSEDED`;
- renderer-owned persistent Doré design truth (Konva/tldraw/Polotno/ComfyUI object state): `SUPERSEDED / REJECTED_DIRECTION`;
- synthetic Design as the production mutation consumer: `SUPERSEDED` for resident mutations, retained only for explicit bounded offline/non-mutating compatibility;
- one-shot native image workspace migration helper/workflow: `RETIRED-AS-ACTION` after incorporation/removal in the merged implementation.

### Evidence boundary

Do not promote this to a live visual-production completion claim until one real purpose-built Westside asset proves the end-to-end loop with real pixels and resident Design application.

The preferred proof remains a purpose-built Doré-derived **light texture** or **Bethlehem-star website grammar asset**, not reuse of an original Doré artwork. Required evidence:

1. real resident/provider generation;
2. durable bytes + checksum/provenance;
3. real vision observations;
4. critic/correction loop where needed;
5. accepted artifact identity;
6. automatic native placement into Doré Design;
7. workspace revision/readback/deterministic verification;
8. before/after visual acceptance evidence.

Until then, this is a verified repository implementation submilestone under active visual/runtime work, not `VIS-GRAMMAR`, Brand V1, A2A, Companion, sparse-runtime or whole-system completion.

### Relation to current visual doctrine

Original Doré works remain curated content. Purpose-built Doré-derived website assets remain the reusable visual grammar. The new native Image→Design path is the correct production substrate for that asset family because it preserves identity/provenance and removes the manual transfer seam without creating a second visual intelligence.

### Relation to P01

No change. P01 remains on its existing subtitle/audio-transcription critical path and is not interrupted or replaced by this visual-runtime work.
