# DORÉ TEMPORARY RECOVERY / FAILURE MARKERS — EVIDENCE LEDGER

Date: 2026-08-28
Sweep: `MEMORY-CONSOLIDATION-SWEEP-01`
Status: ACTIVE EVIDENCE / PARTIALLY RECONCILED

## Purpose

Reconcile the three temporary project markers created on 2026-08-25 against current canonical Search and Conversation Memory evidence without deleting recovery obligations that remain unproved.

Reviewed sources:

- `dore-core/projects/TEMP-BIBLE-SEARCH-FAILURE-SIGNALS-2026-08-25.md`
- `dore-core/projects/TEMP-CONVERSATION-COVERAGE-ANCHOR-2026-08-25.md`
- `dore-core/projects/TEMP-CONVERSATION-MEMORY-GAP.md`
- `dore-core/projects/DORÉ-MASTER-WORK-REGISTER.md`
- current Search / Conversation Memory evidence already represented by `SEARCH`, `CONV-MEM-V1`, `ME-006`, `RQ-003`, and Full Memory Phase 1 M1–M7.

## 1. Bible Search temporary failure signal

Current classification: `ACTIVE / PARTIALLY RECONCILED TEMP EVIDENCE`.

The memo records two real product failures:

- F1: relevant biblical concept/version wording (`Tablets of the Testimony` → KJV `tables of the testimony`) was missed;
- F2: unrelated English text could still produce Scripture results.

Current evidence changes only part of that historical state. Search now has explicit negative-relevance regression protection, so F2 is no longer an unqualified description of current Search behavior. However the existing regression is bounded and does not prove universal abstention/precision. F1's cross-version lexical/concept retrieval case is not evidenced as closed in the current canonical records, and the memo's stronger self-detection/generalization requirement is also not proved.

Disposition:

- retain the memo;
- treat F2 as a historical failure with bounded regression coverage, not as a current universal defect claim;
- keep F1 and autonomous same-class failure detection as open evidence obligations;
- do not delete until the memo's broader unseen recall/precision/abstention and self-detection gates are evidenced.

No new top-level Search workstream is needed: the durable technical/evaluation edge belongs under existing `SEARCH`, `RQ-003` and Search cognition/missing-evidence work.

## 2. Full Conversation Coverage Anchor

Current classification: `ACTIVE / NON-DELETABLE RECOVERY MARKER`.

The anchor's architectural premise has partly advanced: Conversation Memory is now independently represented canonically as `CONV-MEM-V1`, and Full Memory Phase 1 has bounded M1–M7 implementation evidence. Therefore the old concern that full conversation memory had no independent canonical representation is no longer current.

The anchor itself must remain because its deletion gate is source-specific, not architecture-generic. Current evidence does **not** prove that the human-designated ChatGPT checkpoint `p_6a8dcd70d8a48191a237b6716ddc8058` has been accessed and ingested turn-by-turn with stable ordering/provenance, nor that the safety cargo was reconciled against that original transcript.

Disposition:

- retain as active recovery evidence;
- do not infer completion from M1–M7 code alone;
- M8/full-history backfill may become part of the proof path, but it only closes this marker if the designated original source boundary is actually included and verified.

## 3. Conversation Memory Gap memo

Current classification: `PARTIALLY SUPERSEDED ARCHITECTURE SNAPSHOT + ACTIVE RECOVERY CARGO`.

Several 2026-08-25 architecture-gap statements are superseded by later implementation:

- full conversation memory now has a canonical `CONV-MEM-V1` workstream;
- D1/scoped replay, semantic memory, cross-conversation recall, history import and deletion lifecycle have bounded M1–M7 implementation evidence;
- the earlier statement that the conversation-memory path was not independently represented in the Master Register must not be re-read as current truth.

But the file cannot yet be retired/deleted because the second half is a safety-copy cargo and explicitly requires comparison against the original conversation source before closure. No current evidence proves that source-level reconciliation.

Disposition:

- preserve the file;
- treat its early architecture-gap snapshot as historical/partially superseded;
- treat its pending conversation cargo and original-source comparison gate as active recovery obligations;
- do not duplicate its already-canonicalized mission/editorial/distribution/visual ideas into new workstreams unless a still-missing durable obligation is found.

## Canonical interpretation

These temporary files are not three new active projects. They are evidence/recovery markers attached to existing canonical workstreams:

- Bible Search marker → `SEARCH` / `RQ-003` / Search evidence gates;
- Conversation markers → `CONV-MEM-V1` / Full Memory Phase 1 / source-provenance recovery.

This prevents temporary artifacts from silently reappearing as parallel project plans while preserving the exact deletion gates that remain unmet.

## P01 protection

No P01 code, runtime, deployment, subtitle ordering, credential, binding, probe or blocker state was changed by this reconciliation.

## Blocker assessment

No new `HUMAN_DECISION_BLOCKED` or `ENVIRONMENT_BLOCKED` condition is created by this batch. The inability to claim original-source conversation recovery is an evidence gap already bounded by the retained markers, not a new instruction to interrupt the active P01 critical path.