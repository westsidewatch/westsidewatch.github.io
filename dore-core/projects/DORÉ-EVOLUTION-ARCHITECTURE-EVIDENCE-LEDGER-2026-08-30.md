# Doré Evolution / Unified Architecture Evidence Ledger — 2026-08-30

Status: DURABLE SWEEP-01 EVIDENCE
Sweep: `DORÉ-MEMORY-CONSOLIDATION-SWEEP-01.md`
Primary index: `DORÉ-MASTER-WORK-REGISTER.md`

## Bounded evidence reviewed

- `dore-core/evolution/PROJECT.md`;
- `dore-core/evolution/DORÉ-SYSTEM-ARCHITECTURE.md`;
- `dore-core/knowledge/SELF-EQUIPPING-EVOLUTION.md`;
- current canonical `CORE`, `NERVOUS-SYSTEM`, `VIS-GRAMMAR`, `RUNTIME` and `MEM-SWEEP-01` interpretations.

## Classification

### Doré Evolution project

**Current classification:** `CORE/CONTINUOUS / ACTIVE_PARALLEL`.

The project is explicitly permanent and longitudinal. It is not a normal finite project and should not receive an ordinary completion date. Its object is the reusable reflex by which Doré detects a capability gap, researches/evaluates equipment, adapts or builds the missing layer, verifies real work, retains learning and later transfers the method into a materially different domain.

The intended loop is:

`goal → attempt → observe → detect gap → research → equip → execute → verify → learn → transfer`

This is stronger and more specific than a generic statement that Doré should keep learning. It makes **capability acquisition itself** a governed capability frontier.

### Unified system architecture

**Current classification:** `CORE/CONTINUOUS GOVERNING ARCHITECTURE`, not a completed capability claim.

The 2026-08-30 architecture defines six cooperating systems plus an executive control plane:

1. Brain;
2. Memory;
3. Nervous System;
4. Body / Equipment;
5. Evolution Reflex;
6. Vital Signs;
7. Executive / Planner as control plane rather than a seventh organ.

The architecture also introduces a living machine-readable Capability Registry as a required future mechanism for implementation/equipment status, tests, evidence, cost, permissions, failures and replacements.

The document explicitly states `NO BEHAVIORAL EVIDENCE, NO CAPABILITY CLAIM`; therefore architecture existence must not be promoted into evidence that self-equipping autonomy, the registry, or executive control are already operational.

## Relationship to existing canonical work

1. `CORE` already governs learning-through-real-work and capability accumulation, but the Evolution project is durable enough to warrant its own canonical row because it has an explicit permanent project contract and transfer metric.
2. `NERVOUS-SYSTEM` remains the shared capability/authority/observability implementation frontier; the unified architecture does not supersede that workstream. Instead it places Nervous System inside a broader system model.
3. `VIS-GRAMMAR` / Doré Design is named as the first full training ground for the architecture. Design success by itself cannot graduate Evolution; the required stronger proof is later spontaneous transfer of the same capability-acquisition method into a materially different domain.
4. `RUNTIME` continuity is related but distinct: persisted task continuation does not by itself prove autonomous capability acquisition.
5. `SELF-EQUIPPING-EVOLUTION.md` is governing doctrine and is consistent with the Evolution project; neither source should be treated as a completed self-equipping episode.

## Evidence boundary / missing proof

The following remain unproven and must not be claimed as completed:

- one end-to-end self-equipping episode triggered from a real unfamiliar work need without a human prescribing the method;
- autonomous comparison of multiple candidate tools/equipment under one real acceptance test;
- controlled adaptation/installation behind a Doré-owned replaceable interface;
- production acceptance plus provenance/license/cost/security record;
- later reuse of the retained capability without repeating the original human-led discovery;
- blind transfer of the same capability-acquisition reflex into a materially different domain;
- a machine-readable Capability Registry that is actually queried/updated by the Executive;
- evidence that the Executive reliably invokes Evolution rather than merely following a written workflow.

## Smallest high-value future proof

After an active product exposes a genuine new capability gap, persist one bounded episode showing:

`unfamiliar real job → self-detected gap → capability-registry check → autonomous candidate research → identical acceptance test across candidates → controlled selection/adaptation → real-work pass → durable capability/provenance record → later reuse or blind transfer`

The episode should record how much human/ChatGPT procedural intervention was required. A later materially different-domain transfer with no method re-teaching is the strongest graduation evidence.

## Current disposition

- retain Doré Evolution as a permanent `CORE/CONTINUOUS / ACTIVE_PARALLEL` workstream;
- retain the Unified System Architecture as governing architecture, not proof of implementation;
- do not create a fake `VERIFIED_COMPLETE` milestone for Evolution;
- do not rename Researcher 04, Design progress, Runtime continuity or any single successful project into a self-equipping/autonomous-evolution graduation;
- treat the Capability Registry and executive self-equipping loop as `UNKNOWN_NEEDS_EVIDENCE` implementation targets;
- use real product work as training/evaluation grounds without interrupting the P01 subtitle critical path.

## Sweep impact

This bounded pass discovers no new human-decision or environment blocker. P01 state, deployment, credential, binding and execution order were not modified.
