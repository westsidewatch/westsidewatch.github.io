# DORÉ DESIGN MOTION SUPERSESSION — EVIDENCE LEDGER

Date: 2026-09-10
Status: SWEEP-01 EVIDENCE
Parent workstream: `VIS-GRAMMAR`

## Bounded evidence

- commit `966e9832201e7c1d0887d071706637b6ac51afd3` — `DORÉ Design: add new site-native Codrops 8x5 lab page`
- commit `70622499be8560425221fd15d1e91b133550ae2a` — `DORÉ Design: retire old P1 route in favor of new Codrops 8x5 site lab`
- current canonical `DORÉ-MASTER-WORK-REGISTER.md` VIS-GRAMMAR interpretation

## Finding

A concrete motion-language lineage is now evidenced inside Doré Design.

The earlier `motion-p1-living-current` prototype encoded a four-current 8:5 content-flow concept and a click-to-puzzle preview. On 2026-09-10 it was explicitly retired as the live P1 route and replaced by the site-native `motion-codrops-site-8x5` candidate. The replacement preserves an 8:5 card system, adapts the Codrops Grid-to-Full-Preview interaction without iframe dependence, uses Westside product/content identities, and carries its own machine-readable design-experiment principles and acceptance list.

## Classification

- `motion-p1-living-current`: `SUPERSEDED` as the current motion-route implementation; retained as design-history provenance and evidence of the earlier weighted-current concept.
- `motion-codrops-site-8x5`: `ACTIVE_PARALLEL / DESIGN_CANDIDATE` under `VIS-GRAMMAR`; implementation exists, but current evidence does not establish rendered visual acceptance, mobile quality, accessibility/performance acceptance, or production adoption.
- Motion-language route lineage: `COMPLETED_REVISIT_CANDIDATE` governance concern only insofar as historical prototypes can be mistaken for current candidates; do not treat route existence or a commit as design graduation.

## Evidence boundary

The new page is source-level implementation evidence, not proof that its visual/motion language is good enough for the new Westside grammar. Its embedded acceptance list is a specification, not a persisted PASS report. No `VERIFIED_COMPLETE` claim is justified.

The prototype also uses dark preview fields and source-adapted styling. That implementation detail must not be generalized into a new Westside rule; the canonical visual direction remains governed by current `VIS-GRAMMAR` work and subsequent visual evaluation.

## Durable lesson

Doré Design prototypes need an explicit supersession chain. A newly registered experiment can replace an earlier route without erasing the earlier concept; the register should preserve which artifact is current, which is historical, and which acceptance gates remain unproved.

## Smallest next proof

Render the current `motion-codrops-site-8x5` candidate in the real Doré Design environment, verify the stated interaction/8:5/mobile/reduced-motion behavior, and judge it against the current Westside visual principles. Promote, revise or retire it only from that evidence.

P01 impact: NONE.
