# DORÉ A2A / STORYBOOK AUTONOMY EVIDENCE LEDGER — 2026-09-01

Status: SWEEP-01 BOUNDED RECONCILIATION — UPDATED
Classification: one bounded autonomous capability-recovery sub-milestone is `VERIFIED_COMPLETE`; broader self-equipping transfer remains `ACTIVE_PARALLEL / EVALUATION` under `EVOLUTION`, with supporting runtime primitives under `NERVOUS-SYSTEM` and design workload under `VIS-GRAMMAR`.

## Evidence reviewed

Earlier bounded set:

- commit `6dcb391cc91b08b0fdb02b31ec5098932f2ab067` — global component selection / deferred candidates;
- commit `1dd055cd79a9548761788624752fdda5eaa832df` — executable ecosystem-component acceptance harness;
- commit `c6b02a87e904d59b1fa1ac3537e3ca0f0ac25c8b` — queued A2A ↔ Storybook benchmark execution;
- commit `c8fd0597d59d5a2d6d15f8d56dddba01ae374fe4` — queued live autonomous-loop road test.

Newer canonical execution evidence reviewed in this update:

- commit `046a87dc150401288ddc8f362e9c431dd6da8a92` — queued first autonomous capability-loop acceptance;
- commit `0ed626411ecb9ff5075293581edc43a0031a4638` — canonical terminal PASS receipt for `result-autonomous-loop-acceptance-1`;
- commit `f913dab511dbb8b36c1bc08c416e1b5cc62472a4` — first New Westside Storybook real-work loop implementation;
- commit `31c22972d31270f010ab64f3a05baedcde4fe828` — real-work loop execution request;
- commit `bf5a1f7629230c2d5ec9828930296dd11ccc105c` — persisted nonterminal `LEARNING / RESEARCH_REQUIRED` evidence for a Storybook parse failure, preserving parent goal rather than pretending success;
- commit `82b522538d7ed6912cbe5a49ffba3d30880d669a` — threshold-promoted homepage implementation;
- commit `f9883f4bb3440f78f276e95b0ad90b03a059bd71` — visible threshold-promoted V3 activation;
- commit `9d6a6683078c229cb87f3b0380de0afd21e952c4` — explicit verification request for visible homepage V3;
- commit `c7d672c18a063747574b3700e200d1cab8d7a48d` — terminal PASS execution receipt with `VISIBLE_HOMEPAGE_V3_PASS` and product monitor PASS.

## Reconciled interpretation

### 1. A bounded autonomous capability-recovery loop is now genuinely verified

The earlier version of this ledger correctly refused to treat queued execution requests as completion receipts. Newer evidence changes that narrow judgment.

`result-autonomous-loop-acceptance-1` is a persisted terminal PASS. Its receipt records a real capability failure (`@storybook/addon-essentials@^9.1.3` ETARGET), preservation of the parent goal, selection/application of the reusable `storybook9-package-model-repair` skill with provenance, verified repair, retry of the same parent goal, and a successful Storybook build. The receipt explicitly records `autonomous_recovery.state = VERIFIED`, `parent_goal_preserved = true`, and `resumed_parent_goal = true`.

Therefore the following bounded sub-milestone is now defensible:

**`AUTONOMOUS_CAPABILITY_RECOVERY_01` — `VERIFIED_COMPLETE`**

Contract proved in this episode:

`real task failure → reusable capability-gap identification → evidence-bearing repair skill → verified repair → same parent goal resumed → terminal PASS`

This is stronger than architecture text or a mocked CI gate.

### 2. This still does not prove broad transferable self-equipping autonomy

The verified recovery episode is in the Storybook/design-tooling domain and is partly scaffolded by the existing coordination/runtime contract. It does not by itself prove:

- materially different-domain blind transfer without method re-teaching;
- repeated self-detected capability acquisition across unrelated products;
- longitudinal autonomous project selection;
- A2A protocol conformance;
- distributed durable workflow-engine completeness;
- broad `DORÉ_ALIVE_1.0` or equivalent stronger reserved states.

The Master Register `EVOLUTION` requirement should therefore be narrowed from “no persisted end-to-end episode” to: **one bounded real recovery episode is verified; cross-domain blind transfer remains the next decisive gate.**

### 3. The New Westside Storybook lineage adds useful, but mixed, real-work evidence

The real New Westside Storybook loop demonstrates appropriate non-fabrication behavior when execution fails. The persisted `bf5a1f...` receipt records `LEARNING / RESEARCH_REQUIRED`, preserved parent goal, and no false PASS when the generated Storybook story hit a parse error.

Subsequent commits show continued design work, generator repair, Storybook resumes, threshold promotion and a verified visible homepage V3. The `c7d672...` receipt proves the visible V3 wiring/compile/signature checks and product-monitor invariants passed.

This is meaningful execution continuity and design-laboratory evidence. It is **not yet** sufficient to claim that the full original Storybook autonomous road-test contract completed in one uninterrupted autonomous lineage with no hidden human repair. Treat the broader road test as `ACTIVE_PARALLEL / EVALUATION` unless a canonical lineage artifact explicitly closes that contract.

### 4. Runtime primitives remain partial Nervous System evidence

The acceptance harness and receipts provide real evidence for local durability, persisted attempts, parent-goal preservation, retry/learning states, idempotent coordination receipts and product-monitor guardrails. They still do not satisfy the whole shared Nervous System contract or the previously missing A3/A4 authority enforcement across real products.

`NERVOUS-SYSTEM` therefore remains `ACTIVE_PARALLEL`.

### 5. Storybook remains a workload/laboratory, not Brand V1 completion

Storybook and the New Westside homepage are real visual workloads supporting `VIS-GRAMMAR`. The lineage now proves executable comparison/promotion machinery and a visible threshold-promoted homepage implementation, but it does not itself prove the full purpose-built Doré asset suite, rendered D4 visual readback/correction contract, cross-media print transfer, or ecosystem-wide Brand V1 readiness.

`VIS-GRAMMAR` remains `ACTIVE_PARALLEL / BUILDING`; `BRAND-V1` remains gated.

### 6. Explicit non-claims remain governing evidence

No evidence in this bounded review justifies claiming A2A Inspector/TCK conformance, a distributed durable workflow engine, filesystem-event wakeups, cron/orchestrator parity, or adoption of deferred orchestration candidates such as DBOS, Temporal, Prefect, APScheduler or watchfiles.

## Classification / disposition

- `AUTONOMOUS_CAPABILITY_RECOVERY_01`: `VERIFIED_COMPLETE` bounded capability milestone.
- Broader `EVOLUTION` self-equipping transfer: `CORE/CONTINUOUS / ACTIVE_PARALLEL`; now has one real recovery episode, but still lacks materially different-domain blind transfer.
- A2A ↔ Storybook autonomous road test: `ACTIVE_PARALLEL / EVALUATION`; partial real-work lineage with correct LEARNING/RESEARCH_REQUIRED behavior and subsequent product progress, but no single canonical closure artifact found in this bounded review.
- Local durability/lease/idempotency/retry/guardrail/trace primitives: `ACTIVE` implementation evidence supporting `NERVOUS-SYSTEM`; not shared-system completion.
- Storybook/New Westside workload: `ACTIVE_PARALLEL` under `VIS-GRAMMAR`.
- Visible threshold-promoted homepage V3 wiring/compile/product-monitor check: bounded `VERIFIED_COMPLETE` implementation/verification sub-milestone, not visual-language or Brand V1 completion.
- A2A Inspector/TCK conformance: `UNKNOWN_NEEDS_EVIDENCE`.
- Deferred orchestration candidates: `PARKED / CONDITIONAL`.

## Completion evidence still required

For the next stronger `EVOLUTION` claim, require a materially different-domain real job in which Doré, without method re-teaching:

1. detects a reusable capability gap;
2. researches/selects or constructs a bounded adaptation from evidence;
3. verifies the learned capability before promotion;
4. resumes the same parent goal and reaches an accepted terminal state;
5. later reuses that capability or the learned method on another materially different task;
6. preserves provenance and shows no hidden human repair substituted for the claimed autonomous episode.

For the original A2A ↔ Storybook road-test lineage, retain the stricter canonical closure requirement: ecosystem-component acceptance PASS + loop-contract acceptance PASS + real Storybook workload + any RESEARCH_REQUIRED transition + verified knowledge promotion + same-parent-goal resume + canonical terminal result in one traceable lineage.

## Sweep 01 effect

This update changes one important evidence boundary: the statement “no persisted end-to-end recovery episode exists” is now superseded for the bounded Storybook capability-recovery contract. One real recovery episode is verified and should enter the canonical Master Register under `EVOLUTION` as retained evidence.

It does **not** close `EVOLUTION`, `NERVOUS-SYSTEM`, `VIS-GRAMMAR`, the broader A2A Storybook road test, or Sweep 01. No P01 subtitle critical-path state or action is changed.