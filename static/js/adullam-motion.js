(()=>{const root=document.querySelector('[data-adullam-motion]');if(!root)return;const panels=[...root.querySelectorAll('.am-panel')];const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const io=new IntersectionObserver(es=>es.forEach(e=>e.target.classList.toggle('is-visible',e.isIntersecting)),{threshold:.25});panels.forEach(p=>io.observe(p));const enter=root.querySelector('.am-enter'),cover=root.querySelector('.am-cover');enter?.addEventListener('click',()=>{cover.classList.add('is-opening');setTimeout(()=>document.querySelector('#huang')?.scrollIntoView({behavior:reduced?'auto':'smooth'}),520)});if(reduced)return;root.classList.add('am-motion-ready');let ticking=false;const clamp=(n,a=0,b=1)=>Math.max(a,Math.min(b,n));const render=()=>{const vh=innerHeight;for(const p of panels){const r=p.getBoundingClientRect();const progress=clamp((vh-r.top)/(vh+r.height));const enterP=clamp((vh-r.top)/(vh*.72));p.style.setProperty('--p',progress.toFixed(4));p.style.setProperty('--enter',enterP.toFixed(4));}ticking=false};const request=()=>{if(!ticking){requestAnimationFrame(render);ticking=true}};addEventListener('scroll',request,{passive:true});addEventListener('resize',request,{passive:true});render();})();