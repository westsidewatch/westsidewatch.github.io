// DORÉ Cover Resolver: explicit/embedded → source-native → edition identity → ONE fallback.
const CACHE_PREFIX='dore.cover.v1:';const gutenbergId=url=>String(url||'').match(/gutenberg\.org\/ebooks\/(\d+)/i)?.[1]||null;
function cached(id){try{return JSON.parse(localStorage.getItem(CACHE_PREFIX+id)||'null')}catch{return null}}function remember(id,v){try{localStorage.setItem(CACHE_PREFIX+id,JSON.stringify(v))}catch{}return v}
async function imageExists(url){return new Promise(resolve=>{const i=new Image(),done=v=>{i.onload=i.onerror=null;resolve(v)};i.onload=()=>done(true);i.onerror=()=>done(false);i.src=url})}
function explicitCover(book){const c=book?.cover;if(c?.url&&c.mode!=='one-fallback')return {url:c.url,kind:c.kind||'explicit',provider:c.provider||'catalog',provenance:c.provenance||c.url};return null}
async function sourceCover(book){for(const s of book?.sources||[]){const id=gutenbergId(s.url);if(!id)continue;for(const url of [`https://www.gutenberg.org/cache/epub/${id}/pg${id}.cover.medium.jpg`,`https://www.gutenberg.org/cache/epub/${id}/pg${id}.cover.small.jpg`])if(await imageExists(url))return {url,kind:'source-native',provider:'Project Gutenberg',provenance:s.url};}return null}
function identifiers(book){const e=book?.edition||{},w=book?.work||{},ids=e.identifiers||w.identifiers||{};return {isbn:e.isbn||w.isbn||ids.isbn||ids.ISBN,olid:e.olid||w.olid||ids.olid,oclc:e.oclc||w.oclc||ids.oclc,lccn:e.lccn||w.lccn||ids.lccn}}
async function editionCover(book){for(const [type,value] of Object.entries(identifiers(book))){if(!value)continue;const url=`https://covers.openlibrary.org/b/${type}/${encodeURIComponent(value)}-L.jpg?default=false`;if(await imageExists(url))return {url,kind:'edition-identity',provider:'Open Library',identifier:{type,value},provenance:'https://openlibrary.org/dev/docs/api/covers'};}return null}
export async function resolveCover(book){if(!book)return null;const direct=explicitCover(book);if(direct)return direct;const hit=cached(book.id);if(hit?.url&&await imageExists(hit.url))return hit;const resolved=await sourceCover(book)||await editionCover(book);return resolved?remember(book.id,resolved):null}