const JSZIP_URL='https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js';
const JSPDF_URL='https://cdn.jsdelivr.net/npm/jspdf@2.5.2/dist/jspdf.umd.min.js';
const HTML2CANVAS_URL='https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js';
function loadScript(src,test){if(test())return Promise.resolve();return new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=src;s.onload=()=>resolve();s.onerror=()=>reject(new Error(`artifact dependency failed: ${src}`));document.head.appendChild(s)})}
function blobUrl(blob){return URL.createObjectURL(blob)}
function asBlob(content,type){return content instanceof Blob?content:new Blob([content],{type})}
function decodePackage(artifact){const raw=typeof artifact?.content==='string'?JSON.parse(artifact.content):artifact?.content;if(!raw?.files)throw new Error('EPUB package files missing.');return raw}
async function renderPdfBlob(html){await Promise.all([loadScript(JSPDF_URL,()=>Boolean(window.jspdf?.jsPDF)),loadScript(HTML2CANVAS_URL,()=>Boolean(window.html2canvas))]);const frame=document.createElement('iframe');frame.setAttribute('aria-hidden','true');Object.assign(frame.style,{position:'fixed',left:'-20000px',top:'0',width:'794px',height:'1123px',border:'0',background:'#fff'});document.body.appendChild(frame);const doc=frame.contentDocument;doc.open();doc.write(html);doc.close();await new Promise(r=>setTimeout(r,80));if(doc.fonts?.ready)await doc.fonts.ready;const canvas=await window.html2canvas(doc.body,{backgroundColor:'#ffffff',scale:2,useCORS:true,logging:false});frame.remove();const {jsPDF}=window.jspdf,pdf=new jsPDF({unit:'mm',format:'a5',orientation:'portrait'}),pageW=148,pageH=210,imgW=pageW,pxPerMm=canvas.width/pageW,pagePx=Math.floor(pageH*pxPerMm);let y=0,page=0;while(y<canvas.height){const h=Math.min(pagePx,canvas.height-y),slice=document.createElement('canvas');slice.width=canvas.width;slice.height=h;slice.getContext('2d').drawImage(canvas,0,y,canvas.width,h,0,0,canvas.width,h);if(page++)pdf.addPage();pdf.addImage(slice.toDataURL('image/jpeg',0.92),'JPEG',0,0,imgW,h/pxPerMm);y+=h}return pdf.output('blob')}
export async function materializePublicationArtifacts(artifactSet,{coverBlob=null}={}){if(!artifactSet?.web||!artifactSet?.epub||!artifactSet?.pdf)throw new Error('Artifact set incomplete.');await loadScript(JSZIP_URL,()=>Boolean(window.JSZip));const webBlob=asBlob(artifactSet.web.content,'text/html;charset=utf-8'),pkg=decodePackage(artifactSet.epub),zip=new window.JSZip();zip.file('mimetype','application/epub+zip',{compression:'STORE'});zip.file('META-INF/container.xml','<?xml version="1.0"?><container version="1.0" xmlns="urn:oasis:names:tc:opendocument:xmlns:container"><rootfiles><rootfile full-path="OEBPS/content.opf" media-type="application/oebps-package+xml"/></rootfiles></container>');for(const [name,content] of Object.entries(pkg.files))zip.file(name,content);if(coverBlob)zip.file('OEBPS/cover.png',coverBlob);const epubBlob=await zip.generateAsync({type:'blob',mimeType:'application/epub+zip',compression:'DEFLATE'}),pdfBlob=await renderPdfBlob(artifactSet.pdf.renderSource||artifactSet.web.content),files={web:{filename:artifactSet.web.filename,blob:webBlob,url:blobUrl(webBlob)},epub:{filename:artifactSet.epub.filename.replace(/\.epub-package\.json$/,'.epub'),blob:epubBlob,url:blobUrl(epubBlob)},pdf:{filename:artifactSet.pdf.filename,blob:pdfBlob,url:blobUrl(pdfBlob)}};const acceptance={web:webBlob.size>100,epub:epubBlob.size>200,pdf:pdfBlob.size>1000},result={schema:'dore.materialized-publication.v2',status:Object.values(acceptance).every(Boolean)?'accepted':'blocked',acceptance,files};window.__doreMaterializedPublication=result;window.dispatchEvent(new CustomEvent('multiwrite:publication-materialized',{detail:result}));return result}
export function downloadMaterializedPublication(result,formats=['web','epub','pdf']){for(const key of formats){const item=result?.files?.[key];if(!item)continue;const a=document.createElement('a');a.href=item.url;a.download=item.filename;document.body.appendChild(a);a.click();a.remove()}}
