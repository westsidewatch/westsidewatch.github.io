export const LIBRARY_ORIGIN=Object.freeze({PERSONAL:'personal',DAWN:'dawn'});
function makePersonalId(workId='book'){const safe=String(workId||'book').replace(/[^a-z0-9-]+/gi,'-').replace(/^-+|-+$/g,'').toLowerCase()||'book';return `book-dawn-${safe}-${Date.now().toString(36)}`;}
export function personalBookEntry(book={}){return {origin:LIBRARY_ORIGIN.PERSONAL,ownership:'user-local',editable:true,persisted:true,bookId:book.id,title:book.title||'未命名書稿',book};}
export function dawnBookEntry(book={}){const workId=book.workId||book.id;return {origin:LIBRARY_ORIGIN.DAWN,ownership:'canonical-reference',editable:false,persisted:false,catalogId:workId,title:book.work?.title||'未命名',book};}
export function canDeleteEntry(entry){return entry?.origin===LIBRARY_ORIGIN.PERSONAL&&entry.ownership==='user-local';}
export function canEditEntry(entry){return entry?.origin===LIBRARY_ORIGIN.PERSONAL&&entry.editable===true;}
export function canImportToPersonal(entry){return entry?.origin===LIBRARY_ORIGIN.DAWN;}
export function createPersonalReferenceFromDawn(book={}){const workId=book.workId||book.id;if(!workId)throw new Error('Dawn Work ID missing');const now=new Date().toISOString(),work=book.work||{},title=work.title||'未命名',author=work.author||'';return {schema:'multiwrite.book.v2',id:makePersonalId(workId),title,subtitle:author,createdAt:now,updatedAt:now,library:{origin:LIBRARY_ORIGIN.PERSONAL,ownership:'user-local',intake:'dawn-reference',sourceCatalog:'dawn-canonical',canonicalWorkId:workId,sourceStorage:'canonical-pointer',contentDownloaded:false},canonical:{workId},relations:[...(book.relations||[])],import:{mode:'dawn-reference',aiTransformed:false,sourceCount:0,sources:[]},nodes:[{id:`canonical-${String(workId).replace(/[^a-z0-9]+/gi,'-')}`,role:'front_matter',title:'閱讀入口',content:`黎明書局 canonical Work\n\nWork ID: ${workId}\n\n身份、版本、封面與閱讀 Pointer 由 Dawn canonical index 管理。`,sourceFile:'Dawn Canonical',order:0,headingLevel:1,remote:true,canonicalWorkId:workId}]};}
export function libraryLayers({personal=[],dawn=[]}={}){return {personal:personal.map(personalBookEntry),dawn:dawn.map(dawnBookEntry)};}
