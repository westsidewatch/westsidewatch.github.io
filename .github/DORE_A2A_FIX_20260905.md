DORÉ A2A production rollout fix marker.

The GitHub relay now refreshes the Unix control plane from the self-hosted runner checkout before dispatching a capability. `design.production.rollout` prefers `DORE_REPO_ROOT`, avoiding unrelated/diverged personal clones.
