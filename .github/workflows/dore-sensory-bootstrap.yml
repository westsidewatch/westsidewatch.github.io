name: Doré sensory first-party check

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Explain first-party sensory requirement
        run: |
          echo 'Doré sensory memory uses the Cloudflare Pages Function at /api/dore/sensory with a D1 binding named DORE_SENSORY.'
          echo 'No third-party webhook transport is permitted.'
