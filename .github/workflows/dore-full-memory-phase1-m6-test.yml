name: Dore Full Memory Phase 1 M6 Test
on:
  push:
    paths:
      - functions/api/dore/memory.js
      - functions/api/dore/cross-memory.js
      - functions/api/dore/memory-phase1-m6-diagnostic.js
      - .github/workflows/dore-full-memory-phase1-m6-test.yml
  workflow_dispatch:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 40
      - env:
          DORE_HEARTBEAT_TOKEN: ${{ secrets.DORE_HEARTBEAT_TOKEN }}
        run: |
          test -n "$DORE_HEARTBEAT_TOKEN"
          curl --fail-with-body --silent --show-error \
            -X POST \
            -H "Authorization: Bearer $DORE_HEARTBEAT_TOKEN" \
            https://westsidewatch-github-io.pages.dev/api/dore/memory-phase1-m6-diagnostic \
            | tee /tmp/m6.json
      - run: python3 -c "import json; d=json.load(open('/tmp/m6.json')); assert d.get('milestone')=='M6_CROSS_CONVERSATION_MEMORY_PASS',d; print(json.dumps(d,ensure_ascii=False))"
