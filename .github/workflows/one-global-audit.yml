name: ONE Global Audit

on:
  pull_request:
    paths:
      - 'static/one/**'
      - 'scripts/one-global-audit.mjs'
      - '.github/workflows/one-global-audit.yml'
  push:
    branches: [ main ]
    paths:
      - 'static/one/**'
      - 'scripts/one-global-audit.mjs'
      - '.github/workflows/one-global-audit.yml'

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run 66-book / 1189-chapter audit
        id: audit
        continue-on-error: true
        run: node scripts/one-global-audit.mjs
      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: one-global-audit
          path: audit-output/
          if-no-files-found: error
      - name: Persist latest audit report on main
        if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          test -f audit-output/one-global-audit.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add audit-output/one-global-audit.json audit-output/one-global-audit.md
          if git diff --cached --quiet; then
            echo 'Audit report unchanged.'
          else
            git commit -m 'chore: update ONE global audit report'
            git push
          fi
      - name: Enforce audit failures
        if: steps.audit.outcome == 'failure'
        run: exit 1
