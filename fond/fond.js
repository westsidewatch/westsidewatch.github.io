const win=document.querySelector('.window');if(win&&!matchMedia('(prefers-reduced-motion: reduce)').matches){let turn=0;const rest=11800;const duration=1750;const rotate=()=>{const from=turn;const to=turn+180;const start=performance.now();const frame=now=>{const t=Math.min(1,(now-start)/duration);const eased=.5-.5*Math.cos(Math.PI*t);win.style.setProperty('--turn',`${from+(to-from)*eased}deg`);if(t<1)requestAnimationFrame(frame);else{turn=to;setTimeout(rotate,rest)}};requestAnimationFrame(frame)};setTimeout(rotate,rest)}
