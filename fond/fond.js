const threshold=document.querySelector('#threshold');const enter=document.querySelector('#enterFond');const mark=document.querySelector('.mark');const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let turn=0,spinTimer;if(mark&&!reduce){const spin=()=>{const from=turn,to=turn+180,start=performance.now(),duration=1750;const frame=now=>{const t=Math.min(1,(now-start)/duration),e=.5-.5*Math.cos(Math.PI*t);mark.style.setProperty('--turn',`${from+(to-from)*e}deg`);if(t<1)requestAnimationFrame(frame);else{turn=to;spinTimer=setTimeout(spin,11800)}};requestAnimationFrame(frame)};spinTimer=setTimeout(spin,11800)}function openFond(){if(!threshold||threshold.classList.contains('opening'))return;clearTimeout(spinTimer);threshold.classList.add('opening');const delay=reduce?30:1480;setTimeout(()=>{threshold.classList.add('gone');document.body.classList.remove('threshold-locked');document.querySelector('.index')?.scrollIntoView({behavior:reduce?'auto':'smooth',block:'start'})},delay)}enter?.addEventListener('click',openFond);enter?.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openFond()}});
