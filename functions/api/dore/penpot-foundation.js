// controlled Westside foundation write trigger
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
function ev(t){return String(t||'').split(/\n\n+/).map(b=>b.split(/\n/).filter(l=>l.startsWith('data:')).map(l=>l.slice(5).trim()).join('\n')).filter(Boolean).map(x=>{try{return JSON.parse(x)}catch{return null}}).filter(Boolean)}
async function rpc(url,p,sid){const h={'content-type':'application/json','accept':'application/json, text/event-stream'};if(sid)h['Mcp-Session-Id']=sid;const r=await fetch(url,{method:'POST',headers:h,body:JSON.stringify(p)});const t=await r.text();let b=null;try{b=(r.headers.get('content-type')||'').includes('text/event-stream')?(ev(t).find(x=>x?.id===p.id)||ev(t).at(-1)):JSON.parse(t)}catch{}return {ok:r.ok,status:r.status,body:b,sid:r.headers.get('Mcp-Session-Id')||sid||null}}
export async function onRequestGet({env}){const key=String(env?.PENPOT_MCP_KEY||'').trim();if(!key)return json({ok:false,error:'secret_unbound'},503);const url=`https://design.penpot.app/mcp/stream?userToken=${encodeURIComponent(key)}`;try{const init=await rpc(url,{jsonrpc:'2.0',id:1,method:'initialize',params:{protocolVersion:'2025-03-26',capabilities:{},clientInfo:{name:'dore-penpot-foundation',version:'0.1.0'}}});if(!init.ok||init.body?.error)return json({ok:false,stage:'initialize',error:init.body?.error?.message||'failed'},502);await fetch(url,{method:'POST',headers:{'content-type':'application/json','accept':'application/json, text/event-stream',...(init.sid?{'Mcp-Session-Id':init.sid}:{})},body:JSON.stringify({jsonrpc:'2.0',method:'notifications/initialized'})}).catch(()=>null);const code=`const name='00 — WESTSIDE VISUAL CONSTITUTION'; const root=penpot.currentPage.root; const existing=(root.children||[]).find(s=>s.name===name); if(existing) return {created:false,id:existing.id,name:existing.name,width:existing.width,height:existing.height}; const b=penpot.createBoard(); b.name=name; b.x=0; b.y=0; b.resize(1440,1024); b.fills=[{fillColor:'#F2EBDD',fillOpacity:1}]; const title=penpot.createText('WESTSIDE WATCH'); title.name='Brand / Westside Watch'; title.fills=[{fillColor:'#252525',fillOpacity:1}]; b.appendChild(title); title.x=96; title.y=88; const tr=title.getRange(); tr.fontSize='54'; tr.fontWeight='500'; tr.fontFamily='Cormorant Garamond'; tr.letterSpacing='1'; const sub=penpot.createText('Living Paper · Ink Black · First Light Gold'); sub.name='Constitution / Core materials'; sub.fills=[{fillColor:'#9B7A32',fillOpacity:1}]; b.appendChild(sub); sub.x=98; sub.y=166; const sr=sub.getRange(); sr.fontSize='18'; sr.fontWeight='400'; sr.fontFamily='Cormorant Garamond'; sr.letterSpacing='0.5'; const rule=penpot.createRectangle(); rule.name='First Light / Rule'; rule.resize(1246,1); rule.fills=[{fillColor:'#CEBD74',fillOpacity:1}]; b.appendChild(rule); rule.x=97; rule.y=222; const body=penpot.createText('Living Paper is the canvas. Ink Black carries form, not the page. Midnight Blue becomes a field only in true night. First Light Gold touches names, stars, and edges reached by dawn.'); body.name='Constitution / Principle'; body.fills=[{fillColor:'#252525',fillOpacity:0.82}]; b.appendChild(body); body.x=98; body.y=280; body.resize(820,220); const br=body.getRange(); br.fontSize='28'; br.fontWeight='400'; br.fontFamily='Cormorant Garamond'; br.lineHeight='1.35'; return {created:true,id:b.id,name:b.name,width:b.width,height:b.height,children:(b.children||[]).map(s=>({id:s.id,name:s.name,type:s.type}))};`;
const call=await rpc(url,{jsonrpc:'2.0',id:3,method:'tools/call',params:{name:'execute_code',arguments:{code}}},init.sid);if(!call.ok||call.body?.error)return json({ok:false,stage:'execute_code',error:call.body?.error?.message||'failed'},502);return json({ok:true,bridge:'dore.penpot.mcp.v0.1',result:call.body?.result||null})}catch(e){return json({ok:false,stage:'transport',error:String(e?.message||e)},502)}}
