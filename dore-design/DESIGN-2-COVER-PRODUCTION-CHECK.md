# Production check

After A2A rollout, inspect the local result before claiming deployment. The deployed head must include the cover slice and Phase 6 must remain green. The cover's dedicated acceptance endpoint is the second gate; visual acceptance remains a separate human gate.
