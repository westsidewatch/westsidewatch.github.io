# Design 2.0 — Frame + Asset

Status: implemented in the native structured workspace path.

- create frame as a structured block with frame role
- add local images from browser file input using data URLs; no upload service required
- replace an existing image asset without changing the image node identity
- cover / contain / fill
- crop position X/Y in percent
- image and frame geometry remains editable by the same direct-manipulation primitives
- assets remain in the Design workspace and require no paid API or external runtime

Next slice: make Multiwrite cover a real Design asset and complete asset/page workflows.
