# Design 2.0 — first real Multiwrite cover slice

The Design workspace now owns a `多寫 · Cover` page rather than treating the cover as a detached image.

The page is structured, revisioned, rendered from workspace nodes, directly movable, and visible through the same Design editor. It is the first production consumer surface after `multiwrite-home` to use the rebuilt Design 2.0 shell.

Image artwork remains optional and replaceable through local Asset controls; the cover structure itself is not flattened.
