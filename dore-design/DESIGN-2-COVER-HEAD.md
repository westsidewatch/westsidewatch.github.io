# Rollout target

The A2A rollout must deploy the repository head containing `multiwrite-cover`, its renderer, direct manipulation controller, manifest and acceptance probe. A successful older head does not satisfy this gate.
