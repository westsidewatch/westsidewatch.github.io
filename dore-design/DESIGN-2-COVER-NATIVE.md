# Native cover architecture

`multiwrite-cover` is owned by the same canonical structured workspace as the rest of DORÉ DESIGN. Human edits and Doré operations converge on workspace node mutations. The initial cover uses only native text/rule/frame nodes; optional artwork can enter as a local structured image asset without flattening the composition.
