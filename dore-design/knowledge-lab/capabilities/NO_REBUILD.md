# No-rebuild invariant

If an existing Doré capability already performs the required execution correctly, capability discovery references it; it does not duplicate, fork, or replace it. New code is justified only for missing capability boundaries or provider adapters.
