# Free-only policy

Doré capability discovery must not make a paid API a baseline dependency. Providers may be local or publicly free under their documented limits. If a provider later requires paid access for a desired tier, that tier is optional/incompatible with the baseline until the user explicitly changes policy.
