# Discovery contract

Discovery proceeds from broad capability type to service to concrete capability. Planned capabilities are hidden from normal executable discovery. Loading is deferred until the current task needs the capability. Execution remains native/provider-specific.

v0.1 intentionally performs deterministic metadata discovery only; AI-based dynamic routing is not introduced until the small boundary is proven.
