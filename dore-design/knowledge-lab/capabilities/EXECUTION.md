# Execution

Discovery returns metadata. It does not proxy native execution in v0.1. Existing Bible capabilities continue executing through existing Doré Search. Future adapters will translate only at provider boundaries and return provenance-preserving evidence.
