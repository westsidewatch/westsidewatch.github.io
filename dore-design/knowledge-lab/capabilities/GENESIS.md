# Genesis

This component exists because Dawn Library work exposed a broader need: Doré already had Search and Conversation, but future library, academic, heritage, design and memory tools need a safe way to become discoverable without rebuilding existing products.

The first attempted parallel Search design was stopped before merge. Research then shifted to mature capability/tool-discovery patterns, followed by inspection of Doré's existing search implementation. The resulting design is intentionally an enhancement layer.
