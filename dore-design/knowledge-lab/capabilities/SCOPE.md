# v0.1 Scope Lock

In scope: capability metadata, discovery, deferred loading contract, acceptance.

Out of scope: Search UI changes, Conversation changes, replacing search execution, external provider execution, AI routing, paid APIs.
