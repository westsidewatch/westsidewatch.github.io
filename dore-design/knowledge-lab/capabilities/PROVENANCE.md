# Provenance

Every future discovery result must retain provider/source identity and enough source metadata for Doré or a human to verify where the result came from. Cross-provider normalization must never erase provenance.
