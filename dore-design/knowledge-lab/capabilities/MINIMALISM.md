# Low-load constraint

v0.1 uses JSON plus Python standard library only. It adds no resident service, database, browser process, model call, or third-party runtime dependency. This keeps the enhancement negligible beside Doré's existing Mac workload.
