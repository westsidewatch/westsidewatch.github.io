# Doré lesson

When a new project needs a capability, first inspect what already exists. Research mature patterns only until a minimal hypothesis is possible. Add the smallest compatible layer, prove it without damaging the old capability, then promote. A rejected architecture is useful evidence when its reason is preserved.
