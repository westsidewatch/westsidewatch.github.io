# Acceptance transition

The branch must now be evaluated, not expanded. A passing CI result proves repository-level invariants; Doré's canonical local result proves the Mac-side integration boundary. Both are required before merge/promotion.
