# Multiwrite Cover rollout

Ready for production rollout through the existing DORÉ A2A control plane.

Acceptance after rollout must show:
1. rollout `ok:true`, `status:completed`;
2. production head includes this slice;
3. Phase 6 specimen remains PASS;
4. resident cover acceptance endpoint reports `ok:true`.
