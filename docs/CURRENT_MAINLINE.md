# CURRENT MAINLINE

Status: ACTIVE CHECKPOINT

This file exists so Doré, Codex, A2A workers, and future engineering sessions can always recover the current product mainline before following temporary branches.

## Current mainline

The active product mainline is Paradise Cinema / 天堂電影院.

1. Continue the canonical Bible-media resource framework.
2. Make Moment first-class: exact media time segments tied to Scripture, Biblical Event, Person, Place, Period, and Theme coordinates.
3. Preserve the principle: **Bible is the Index; media is the projection.**
4. Use the same Bible index as the future shared coordinate substrate for ONE, Dawn Library / 黎明書局, 多寫, Search, and the main site.
5. After the Paradise Cinema resource framework reaches the intended stage, return to the formally established project **Doré Emergence / 浮現**.

## Emergence checkpoint

Emergence is not a generic recommendation effect. Its authoritative path is:

`Context -> Bible Coordinate -> Canonical Relation -> Resource -> Emergence`

Semantic/fuzzy retrieval may discover candidates, but Bible-coordinate evidence is the durable reason a resource is allowed to rise from trace/whisper to visible interaction.

## Temporary detour

We are temporarily leaving the product mainline to establish the engineering-index foundation:

- one global repository entrypoint;
- a machine-readable System Atlas / system catalog;
- stable identities for products, capabilities, datasets, runtime surfaces, and architecture records;
- ADRs for durable decisions;
- links from Memory and A2A back to canonical GitHub identities;
- CI acceptance so cross-system engineering cannot silently bypass the catalog.

The repository already contains `DORÉ-MASTER-WORK-REGISTER.md` and the active `DORÉ-MEMORY-CONSOLIDATION-SWEEP-01.md`. This detour does **not** start a second memory sweep. System Atlas supplies the stable engineering coordinates that the existing sweep was missing, so its scattered findings can be attached to canonical entities and brought home without erasing provenance.

This detour is infrastructure, not a change of product direction.

## Return condition

Return to the Paradise Cinema mainline once the System Atlas foundation and its first reconciliation pass are usable enough that a new AI/agent/session can reliably answer:

- What is the main site structure?
- What products and capabilities exist?
- Where is each canonical record?
- Which branch/PR/decision is current versus historical?
- What is the current product mainline?
- What does the Master Work Register track versus what System Atlas identifies?
- What does A2A execute, and what does Memory remember?

The engineering index exists to prevent project knowledge from becoming another collection of disconnected documents.
