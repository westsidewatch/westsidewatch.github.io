# C04 page-2 C/D mature light trial

Runtime implementation is in `dore-design/living_water_candidate_04.py`.

The page-2 switch exposes three direct visual states over the same C04 content and Doré background: C = `three-good-godrays` live mature reference, D = React Bits `LightRays` live mature reference, Off = no third-party light layer. This is an experiment surface, not a production dependency decision.