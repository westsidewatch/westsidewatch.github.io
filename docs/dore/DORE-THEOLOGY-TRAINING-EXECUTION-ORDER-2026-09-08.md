# Doré Theology Training Execution Order — 2026-09-08

Training MUST NOT precede runtime theological sovereignty enforcement.

Order:

1. merge and integrate Theology Rails into the actual AI-dialogue inference/display path;
2. prove pre-inference authority/context control and post-inference admission on the real Mac;
3. run zero-training Gemma 4 E4B baseline;
4. run read-only MLX readiness probe;
5. prepare quarantined bilingual alignment splits outside Doré stores;
6. train 32-example adapter only;
7. run blind held-out bilingual evaluation and adapter-load fingerprint;
8. continue 64/128/256 only if material improvement is demonstrated;
9. admit only adapter artifact + abstract evaluation result after gates pass; never ingest quarantine corpus.

This ordering is a hard safety gate, not a scheduling preference.
